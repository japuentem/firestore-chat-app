import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beaversoft.fireChatApp',
  appName: 'firestore-chat-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
